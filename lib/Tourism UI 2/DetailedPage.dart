import 'package:flutter/material.dart';

class TUIPage2 extends StatelessWidget {
  const TUIPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
